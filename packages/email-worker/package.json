{
  "name": "@mailbase/email-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "@mailbase/shared": "*",
    "drizzle-orm": "^0.45.2",
    "postal-mime": "^2.4.0"
  }
}
